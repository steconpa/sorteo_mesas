// Copia este archivo como config.js (mismo directorio) y completa los valores reales.
// config.js NO debe subirse al repositorio (está en .gitignore).
const CONFIG = {
  APPS_SCRIPT_URL: "URL_DEL_WEB_APP_AQUI",
  APP_TOKEN: "TOKEN_SECRETO_AQUI"
};
